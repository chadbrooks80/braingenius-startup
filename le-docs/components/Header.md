# Header

Sticky Brain Genius branding and navigation shell shown when module settings enable the header.

The current component is static and receives no props. Its word-list, progress, username, and logout elements are placeholders rather than interactive controls. Authentication and application navigation must be supplied by a later integration rather than embedded into the shared Learning Engine.

Source: `src/components/Blocks/Header.tsx`.
