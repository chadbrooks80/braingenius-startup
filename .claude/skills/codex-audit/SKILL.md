Run in terminal:

codex exec -C . -s read-only "Do a Feature Audit. Open ./.claude/skills/audit/SKILL.md, follow it exactly, and audit this project. Strictly output findings only."

once it is complete output the final review results here.
